create database test;
create table user(
ID		varchar(10),
NAME	varchar(10)
);


insert into user value('test01', 'test01');

drop table user;
---------------------------------------------
CREATE DATABASE 	ANDROID;

USE 	ANDROID	;

CREATE TABLE 	users(
uemail varchar(10),
uname varchar(10),
upassword varchar(10)
);